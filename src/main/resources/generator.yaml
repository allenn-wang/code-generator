moduleName: demo

modules:
  demo:
    author: allenn wang
    rootPackageName: com.allenwang
    dataBaseName: demo
    moduleName: demo
    coreProjectName:
    commonProjectName:
    cacheEnable: false
    swaggerEnable: false
    db:
      url: jdbc:mysql://demo:3306/demo
      username: demo
      password: demo
    path:
      controller: controller
      service: service
      impl: service.impl
      dao: dao
      entity: entity
      mapper: mapper
      bo: bo
    systemColumns:
      - add_time
      - add_user
      - update_time
      - update_user
      - is_delete
      - is_inner
    buildTables:
      - test_table

    cacheTables:
      - test_table
